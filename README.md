 run `npm run build` command, you can build the project now! you will find out the project will be published in `dist` directory,`js`has combined and optimized, `es6+` has converted to `es5`. all image file has been optimized. 
